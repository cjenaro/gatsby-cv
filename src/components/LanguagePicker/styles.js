import styled from "styled-components"

export const StyledLanguagePicker = styled.div`
  button {
    border: 0;
    background: none;

    &.selected {
      font-weight: 600;
    }
  }

  span {
    font-size: 24px;
  }
`
